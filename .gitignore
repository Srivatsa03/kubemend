__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.DS_Store
*.kubeconfig
snapshots/

# incident logs are local state, not source
*.db
*.db-journal

# LaTeX build artefacts (the PDF is committed deliberately)
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz

# the demo recording is source, not an artifact
!docs/media/demo.cast

# Post drafts and recording scratch: personal working material, not the project.
# Kept on disk, deliberately not published.
docs/posts/
docs/media/render.py
